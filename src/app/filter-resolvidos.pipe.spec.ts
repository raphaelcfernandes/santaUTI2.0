import { FilterResolvidosPipe } from './filter-resolvidos.pipe';

describe('FilterResolvidosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterResolvidosPipe();
    expect(pipe).toBeTruthy();
  });
});
